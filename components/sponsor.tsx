const Sponsor = () => {
    return (
        <section className="" id="sponsor" aria-label="sponsor Abschnitt">
                Sponsor
        </section>  
            );
        };

export default Sponsor;