const Schedule = () => {
    return (
        <section className="" id="schedule" aria-label="schedule Abschnitt">
                Schedule
        </section>  
            );
        };

export default Schedule;